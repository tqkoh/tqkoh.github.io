# section information
section:
  name: Recent Posts
  id: recent-posts
  enable: false
  weight: 60
  showOnNavbar: true
  # Can optionally hide the title in sections
  # hideTitle: true
# no additional configuration is required
